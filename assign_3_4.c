#include<stdio.h>
int main()
{ int a,b,c;
	printf("\ngive three numbers");
	scanf("%d%d%d",&a,&b,&c);
	if((a*a)==(b*b)+(c*c))
		printf("rt",a=90);
	else
	{if((a*a)!=(b*b)+(c*c))
		printf("not rt",a!=90);}
}
				

