#include<stdio.h>
{
	int a,b,c,p,y;
       printf("the eq is f(x)=a*x*x+b*x+c");
       printf("\nEnter the value of a, b, c, p=");
       scanf("%d %d %d %d",&a,&b,&c,&p);
       x=p;
       y=a*x*x+b*x+c;
       printf("f(p)=%d",y);
}
